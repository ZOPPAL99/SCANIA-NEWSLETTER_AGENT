# Reference Documents

- Newsletter schema in `src/schemas/newsletter.ts` is the source of truth.
- QA checks in `src/qa/validators.ts` define hard business constraints.
- Renderer outputs must be deterministic and generated from schema data only.
